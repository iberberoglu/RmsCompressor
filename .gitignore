# macOS
**/.DS_Store

# Derleme çıktıları — diskte kalır, git'te tutulmaz (yüzlerce MB binary)
Builds/MacOSX/build/
Builds/VisualStudio2022/x64/
Builds/VisualStudio2022/*.user
**/*.o
**/*.a

# Xcode kullanıcıya özel durum dosyaları
**/xcuserdata/
**/*.xcuserstate

# Okul sunumu / rapor dosyaları — depo dışında tutulur (rapor, .key, .pages, demo Live seti)
Serbest Proje Çalışması*/
_git_yedek_*/

# Kişisel çalışma notları — depoya ve GitHub'a asla girmesin
YAPILACAKLAR.md
**/YAPILACAKLAR.md

# Test ses malzemesi — diskte kalır, depoda tutulmaz
*.wav
*.aif
*.aiff

# DAW önbellekleri (Ableton analizi, Reaper dalga formu)
*.asd
*.reapeaks
peaks/

# AudioPluginHost test oturumları — mutlak yol içeriyor, taşınabilir değil
*.filtergraph

# Öğrenirken incelenen üçüncü taraf proje — bu depoya ait değil
AudioFilePlayer-master/
