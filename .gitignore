# macOS
**/.DS_Store

# Derleme çıktıları — diskte kalır, git'te tutulmaz (yüzlerce MB binary)
Builds/MacOSX/build/
Builds/VisualStudio2022/x64/
Builds/VisualStudio2022/*.user
**/*.o
**/*.a

# Xcode kullanıcıya özel durum dosyaları
**/xcuserdata/
**/*.xcuserstate

# Okul sunumu / rapor dosyaları — depo dışında tutulur (rapor, .key, .pages, demo Live seti)
Serbest Proje Çalışması*/
_git_yedek_*/
