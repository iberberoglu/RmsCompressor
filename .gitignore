# macOS
**/.DS_Store

# Build output. Stays on disk, never in the repo (hundreds of MB of binaries).
Builds/MacOSX/build/
Builds/VisualStudio2022/x64/
Builds/VisualStudio2022/*.user
**/*.o
**/*.a

# Xcode per-user state
**/xcuserdata/
**/*.xcuserstate

# University report and presentation files, kept outside the repo
Serbest Proje Çalışması*/
_git_yedek_*/

# Personal working notes
YAPILACAKLAR.md
**/YAPILACAKLAR.md

# Test audio. Stays on disk, not tracked.
*.wav
*.aif
*.aiff

# DAW caches (Ableton analysis, Reaper peaks)
*.asd
*.reapeaks
peaks/

# AudioPluginHost sessions. They embed absolute paths, so they are not portable.
*.filtergraph

# A third-party project I studied while learning. Not part of this repo.
AudioFilePlayer-master/
